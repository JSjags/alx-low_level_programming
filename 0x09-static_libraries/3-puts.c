#include "main.h"
#include<stdio.h>
/**
(* _puts - Prints string on a new line
 * main - Entry point
 *)
 * Return: Always 0 (Success)
 */
void _puts(char *str)
int main(void)
{
	puts("\nProgramming is like building a multilingual puzzle");
	return (0);
}
